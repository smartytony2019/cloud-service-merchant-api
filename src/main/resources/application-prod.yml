server:
  port: 13000
  servlet:
    context-path: /gw-merchant-api

#Feign请求超时时间
ribbon:
  ReadTimeout: 60000
  ConnectTimeout: 60000

#开启熔断器
feign:
  sentinel:
    enabled: true

spring:
  application:
    name: cloud-service-merchant-api

  cloud:
    #熔断器
    sentinel:
      transport:
        port: ${server.port}
        dashboard: 45.125.218.106:8002

    #nacos配置
    nacos:
      discovery:
        server-addr: 45.125.218.106:8848

  #数据库配置
  datasource:
    #    url: jdbc:mysql://127.0.0.1:3306/spring-boot-demo?useUnicode=true&characterEncoding=UTF-8&useSSL=false&autoReconnect=true&failOverReadOnly=false&serverTimezone=GMT%2B8
    url: jdbc:mysql://103.84.110.202:3306/cloud?useUnicode=true&characterEncoding=UTF-8&useSSL=false&autoReconnect=true&failOverReadOnly=false&serverTimezone=GMT%2B8
    username: root
    password: ^qaz123@hm!@#$
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    initialization-mode: always
    continue-on-error: true
    #    schema:
    #      - "classpath:db/schema.sql"
    #    data:
    #      - "classpath:db/data.sql"
    hikari:
      minimum-idle: 5
      connection-test-query: SELECT 1 FROM DUAL
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 30000
      pool-name: SpringBootDemoHikariCP
      max-lifetime: 60000
      connection-timeout: 30000

logging:
  level:
    com.xinbo: debug
  file:
    max-size: 5MB
  path: ./logs/


zookeeper:
  server-addr: 45.125.218.106:2181,45.125.218.106:2182,45.125.218.106:2183
  session-timeout: 120000

rocketmq:
  name-server: 45.125.218.106:9876
  producer-group: sport_rocketmq_producer_group
  producer-timeout: 300000